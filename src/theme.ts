// Shared UI palette used across renderer components (single source of truth so
// the same brand value isn't redefined per file).

/** Claude brand accent — the YOLO preset color, the color-input placeholder
 *  hint, and the account-wide usage chip's bar. */
export const CLAUDE_BRAND_COLOR = "#d97757";
